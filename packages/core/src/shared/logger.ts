/**
 * Re-export the shared logger factory for Vite integration.
 *
 * @example
 * import { createLogger } from 'mokup/vite'
 *
 * const logger = createLogger()
 */
export { createLogger } from '@mokup/shared/logger'

/**
 * Re-export mokup CLI helpers.
 *
 * @example
 * import { buildManifest } from 'mokup/cli'
 *
 * const result = await buildManifest({ dir: 'mock', outDir: '.mokup' })
 */
export * from '@mokup/cli'

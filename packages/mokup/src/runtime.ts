/**
 * Re-export the mokup runtime API.
 *
 * @example
 * import { createRuntime } from 'mokup/runtime'
 *
 * const runtime = createRuntime({ manifest: { version: 1, routes: [] } })
 */
export * from '@mokup/runtime'

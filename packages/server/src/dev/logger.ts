/**
 * Re-export the shared logger factory for server dev tools.
 *
 * @example
 * import { createLogger } from '@mokup/server'
 *
 * const logger = createLogger()
 */
export { createLogger } from '@mokup/shared/logger'

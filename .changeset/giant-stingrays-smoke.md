---
"@mokup/playground": patch
---

Enable docs SW registration via env override, expand playground layout to fill the viewport, add a VS Code open shortcut for routes in dev, and consolidate playground colors into theme tokens.

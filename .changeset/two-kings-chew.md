---
'@mokup/shared': patch
---

Export `collectSwConflictDiagnosticWarning()` and `createSwConflictDiagnosticSections()` so service worker diagnostics can reuse one shared implementation across mokup packages.

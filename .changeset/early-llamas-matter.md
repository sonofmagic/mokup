---
'mokup': patch
'@mokup/cli': patch
'@mokup/server': patch
---

Add repeatable `mokup serve --error-on <category>` support and allow fetch-based server startup to fail on selected route diagnostics.

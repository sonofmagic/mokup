---
'mokup': patch
'@mokup/cli': patch
'@mokup/core': patch
---

Add `errorOn` diagnostics controls so selected mokup route and service worker diagnostics can fail builds instead of only logging warnings.

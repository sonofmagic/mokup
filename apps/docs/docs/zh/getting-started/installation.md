# 安装

## 前置要求

- Node.js 20+
- pnpm（推荐）

## 安装依赖

开发工具（Vite 插件 + CLI）：

```bash
pnpm add -D mokup
```

运行时/服务端适配器（部署或中间件）：

```bash
pnpm add mokup
```

如果你只做本地 Vite 开发，把 `mokup` 装在 devDependencies 即可。

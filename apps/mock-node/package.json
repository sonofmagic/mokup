{
  "name": "mock-node",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:manifest": "pnpm --filter @mokup/cli build && mokup build --dir mock --out dist",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "hono": "^4.9.0",
    "mokup-hono": "workspace:*"
  },
  "devDependencies": {
    "@mokup/cli": "workspace:*",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}

{
  "name": "mock-node",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:manifest": "pnpm --filter moku-cli build && moku build --dir mock --out dist",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "hono": "^4.9.0",
    "moku-hono": "workspace:*"
  },
  "devDependencies": {
    "moku-cli": "workspace:*",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
